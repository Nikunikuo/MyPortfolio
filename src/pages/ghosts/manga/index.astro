---
import Icon from "../../../components/Icon.astro";

const pageTitle = "GHOSTS - 漫画";
const pageDescription = "GHOSTSの物語をコミックで体験（準備中）";

// エピソード一覧（今後追加予定）
const episodes = [
  // Coming Soon
];
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=5.0"
    />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDescription} />
    <meta
      name="keywords"
      content="GHOSTS, 漫画, コミック, AI統治, 2163年, サイバーパンク, ディストピア, マンガ, ビジュアル"
    />
    <meta name="author" content="大鹿ニク / M1RA" />
    <meta name="robots" content="index, follow, max-image-preview:large" />
    <meta name="theme-color" content="#0F0F1E" />
    <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Open Graph -->
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:url" content={new URL(Astro.url.pathname, Astro.site)} />
    <meta property="og:type" content="article" />
    <meta property="og:site_name" content="GHOSTS - niku.studio" />
    <meta
      property="og:image"
      content={new URL("/images/GHOSTS/logo.webp", Astro.site)}
    />
    <meta property="og:locale" content="ja_JP" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={pageDescription} />
    <meta
      name="twitter:image"
      content={new URL("/images/GHOSTS/logo.webp", Astro.site)}
    />
    <meta name="twitter:site" content="@M1RA_A_Project" />

    <!-- JSON-LD -->
    <script
      type="application/ld+json"
      set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "CreativeWork",
        name: pageTitle,
        description: pageDescription,
        url: new URL(Astro.url.pathname, Astro.site).toString(),
        author: {
          "@type": "Person",
          name: "大鹿ニク / M1RA",
        },
        genre: "サイバーパンク漫画",
        inLanguage: "ja",
        isPartOf: {
          "@type": "WebSite",
          name: "GHOSTS",
          url: new URL("/ghosts/", Astro.site).toString(),
        },
      })}
    />
  </head>
  <body class="ghosts-body">
    <!-- Floating Logo -->
    <a href="/ghosts/" class="floating-logo">
      <img src="/images/GHOSTS/logo.webp" alt="GHOSTS" />
    </a>

    <main>
      <div class="novel-container">
        <!-- Header -->
        <section class="novel-header">
          <h1 class="page-title">GHOSTS - 漫画</h1>
          <p class="page-description">
            GHOSTSの世界観をコミックで描く壮大な物語
          </p>
        </section>

        <!-- Synopsis Section -->
        <section class="synopsis-section">
          <div class="synopsis-content">
            <p>
              2077年、統治AI〈ARCHIVE〉の事故によって生まれた放棄地区〈デッドゾーン〉。<br
              />そこに取り残されたのは、触れたものに生命力を分け与える不思議な少女ヒナと、<br
              />彼女を守るために規格外の行動を取った第一世代ヒューマノイド・ノアだった。
            </p>
            <p>
              それから約九十年。<br
              />若い姿のまま老いていくヒナと、姿を変えないノアは、<br
              />誰も近づかない廃墟を歩きながら、「あの日」の真相と失われた記録〈アーカイブ〉を追い始める。
            </p>
            <p>
              デッドゾーンの奥で、最後の生存者たちによる長いサバイバルが動き出す。
            </p>
          </div>
        </section>

        <!-- Episodes List or Coming Soon -->
        <section class="episodes-section">
          {
            episodes.length > 0 ? (
              episodes.map((episode) => (
                <article class="episode-card">
                  <div class="episode-header">
                    {episode.isFree && (
                      <span class="episode-badge">無料公開</span>
                    )}
                    <span class="episode-number">EP.{episode.number}</span>
                  </div>
                  <div class="episode-content">
                    <h2 class="episode-title">{episode.title}</h2>
                    <p class="episode-description">{episode.description}</p>
                    <div class="episode-footer">
                      {episode.noteUrl ? (
                        <a
                          href={episode.noteUrl}
                          target="_blank"
                          rel="noopener noreferrer"
                          class="note-link"
                        >
                          <Icon icon="paper-plane-tilt" size="1.2rem" />
                          <span>読む</span>
                          <span class="link-arrow">→</span>
                        </a>
                      ) : (
                        <div class="coming-soon-label">
                          <Icon icon="rocket-launch" size="1.2rem" />
                          <span>{episode.status}</span>
                        </div>
                      )}
                    </div>
                  </div>
                </article>
              ))
            ) : (
              <div class="coming-soon-message">
                <Icon icon="book-open-text" size="5rem" />
                <h2 class="coming-soon-title">COMING SOON</h2>
                <p class="coming-soon-text">
                  GHOSTSの物語を漫画化するプロジェクトが進行中です。
                  <br />
                  AI統治下の世界で音楽を取り戻す4人の戦いを、ビジュアルで体験できる日をお楽しみに。
                </p>
              </div>
            )
          }
        </section>

        <!-- Back Link -->
        <div class="back-section">
          <a href="/ghosts/" class="back-link">← GHOSTSトップに戻る</a>
        </div>
      </div>
    </main>
  </body>
</html>

<style is:global>
  /* GHOSTS Body */
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  .ghosts-body {
    background: #0f0f1e;
    color: #e0e0e0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
      sans-serif;
    line-height: 1.6;
    overflow-x: hidden;
  }

  /* Floating Logo */
  .floating-logo {
    position: fixed;
    top: 2rem;
    left: 2rem;
    z-index: 100;
    transition: all 0.3s ease;
  }

  .floating-logo img {
    height: 60px;
    width: auto;
    filter: drop-shadow(0 0 10px rgba(255, 8, 68, 0.5));
    transition: all 0.3s ease;
  }

  .floating-logo:hover img {
    filter: drop-shadow(0 0 15px rgba(39, 225, 255, 0.8));
    transform: scale(1.05);
  }

  @media (max-width: 768px) {
    .floating-logo {
      top: 1rem;
      left: 1rem;
    }

    .floating-logo img {
      height: 44px;
    }
  }

  main {
    padding-top: 100px;
  }

  /* Scrollbar */
  ::-webkit-scrollbar {
    width: 10px;
  }

  ::-webkit-scrollbar-track {
    background: #0f0f1e;
  }

  ::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, #ff0844, #27e1ff);
    border-radius: 5px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(180deg, #27e1ff, #ff0844);
  }

  ::selection {
    background: #ff0844;
    color: white;
  }
</style>

<style>
  .novel-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem;
  }

  /* Header */
  .novel-header {
    text-align: center;
    margin-bottom: 4rem;
    padding: 2rem 0;
  }

  .page-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    color: #ff0844;
    margin-bottom: 1.5rem;
    letter-spacing: 0.2rem;
    text-transform: uppercase;
    text-shadow:
      0 0 20px rgba(255, 8, 68, 0.8),
      0 0 40px rgba(255, 8, 68, 0.5);
  }

  .page-description {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: #b0b0b0;
    line-height: 1.8;
  }

  /* Synopsis Section */
  .synopsis-section {
    max-width: 800px;
    margin: 0 auto 4rem;
    padding: 2rem;
    background: rgba(255, 255, 255, 0.03);
    border-left: 2px solid rgba(255, 255, 255, 0.1);
    border-right: 2px solid rgba(255, 255, 255, 0.1);
    position: relative;
  }

  .synopsis-section::before,
  .synopsis-section::after {
    content: "";
    position: absolute;
    width: 20px;
    height: 2px;
    background: rgba(255, 255, 255, 0.2);
    transition: width 0.3s ease;
  }

  .synopsis-section::before {
    top: 0;
    left: 0;
  }
  .synopsis-section::after {
    bottom: 0;
    right: 0;
  }

  .synopsis-section:hover::before,
  .synopsis-section:hover::after {
    width: 100%;
    background: rgba(39, 225, 255, 0.5);
  }

  .synopsis-content p {
    font-size: 0.95rem;
    line-height: 2;
    color: #ccc;
    margin-bottom: 1.5rem;
    text-align: justify;
  }

  .synopsis-content p:last-child {
    margin-bottom: 0;
  }

  /* Episodes Section */
  .episodes-section {
    margin-bottom: 4rem;
  }

  /* Episode Card - コンパクト版 */
  .episode-card {
    background: rgba(15, 15, 30, 0.5);
    border-left: 3px solid rgba(255, 8, 68, 0.3);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
    position: relative;
  }

  .episode-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 3px;
    height: 100%;
    background: linear-gradient(180deg, transparent, #ff0844, transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .episode-card:hover::before {
    opacity: 1;
  }

  .episode-card:hover {
    background: rgba(15, 15, 30, 0.8);
    transform: translateX(5px);
  }

  .episode-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .episode-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: rgba(0, 255, 136, 0.2);
    border: 1px solid #00ff88;
    color: #00ff88;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    border-radius: 0.25rem;
  }

  .episode-number {
    font-size: 0.75rem;
    color: #666;
    font-weight: 600;
    letter-spacing: 0.1em;
  }

  .episode-content {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .episode-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: #ffffff;
    margin: 0;
    letter-spacing: 0.02em;
    transition: color 0.3s ease;
  }

  .episode-card:hover .episode-title {
    color: #27e1ff;
  }

  .episode-description {
    font-size: 0.9rem;
    color: #888;
    line-height: 1.6;
    margin: 0;
  }

  .episode-footer {
    display: flex;
    align-items: center;
    margin-top: 0.5rem;
  }

  .note-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #ff0844;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition: all 0.3s ease;
  }

  .note-link:hover {
    color: #27e1ff;
  }

  .link-arrow {
    transition: transform 0.3s ease;
  }

  .note-link:hover .link-arrow {
    transform: translateX(3px);
  }

  .coming-soon-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #ffb700;
    font-weight: 600;
    font-size: 0.8rem;
  }

  /* Coming Soon Message */
  .coming-soon-message {
    text-align: center;
    padding: 3rem 2rem;
    background: rgba(20, 20, 20, 0.6);
    border: 2px solid rgba(255, 8, 68, 0.3);
    border-radius: 0.5rem;
  }

  .coming-soon-message [astro-icon] {
    color: #ffb700;
    opacity: 0.8;
    margin-bottom: 1.5rem;
  }

  .coming-soon-title {
    font-size: clamp(1.75rem, 3.5vw, 2.5rem);
    font-weight: 900;
    color: #27e1ff;
    margin-bottom: 1rem;
    letter-spacing: 0.2rem;
    text-shadow:
      0 0 15px rgba(39, 225, 255, 0.5),
      0 0 30px rgba(39, 225, 255, 0.3);
  }

  .coming-soon-text {
    font-size: 0.95rem;
    color: #b0b0b0;
    line-height: 1.8;
  }

  /* Back Section */
  .back-section {
    text-align: center;
    margin-top: 4rem;
  }

  .back-link {
    display: inline-block;
    padding: 1rem 2rem;
    background: rgba(39, 225, 255, 0.1);
    border: 2px solid rgba(39, 225, 255, 0.3);
    border-radius: 0.5rem;
    color: #27e1ff;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    letter-spacing: 0.1em;
    transition: all 0.3s ease;
  }

  .back-link:hover {
    background: rgba(39, 225, 255, 0.2);
    border-color: #27e1ff;
    box-shadow: 0 0 20px rgba(39, 225, 255, 0.3);
    transform: translateX(-5px);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .novel-container {
      padding: 1rem;
    }

    .episode-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .coming-soon-message {
      padding: 3rem 1.5rem;
    }
  }
</style>
