---
import Icon from '../../../components/Icon.astro';

const pageTitle = 'GHOSTS - アニメ';
const pageDescription = '動くGHOSTSの世界 - ショートアニメと長編アニメで展開';

// ショートアニメ一覧
const shortAnimes = [
  {
    number: 1,
    title: 'ヒナ＆ノア＆プルケ',
    videoPath: '/images/GHOSTS/Anime/01.mp4',
  },
  {
    number: 2,
    title: 'AIs',
    videoPath: '/images/GHOSTS/Anime/02.mp4',
  },
  {
    number: 3,
    title: 'リブート紹介',
    videoPath: '/images/GHOSTS/Anime/reboot.mp4',
  },
];

// 長編アニメ一覧
const longAnimes = [
  // Coming Soon
];
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0" />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDescription} />
    <meta name="keywords" content="GHOSTS, アニメ, ショートアニメ, 長編アニメ, アニメーション, AI統治, 2163年, サイバーパンク, ディストピア, 動画, エピソード" />
    <meta name="author" content="大鹿ニク / M1RA" />
    <meta name="robots" content="index, follow, max-image-preview:large" />
    <meta name="theme-color" content="#0F0F1E" />
    <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Open Graph -->
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:url" content={new URL(Astro.url.pathname, Astro.site)} />
    <meta property="og:type" content="video.other" />
    <meta property="og:site_name" content="GHOSTS - niku.studio" />
    <meta property="og:image" content={new URL('/images/GHOSTS/logo.webp', Astro.site)} />
    <meta property="og:locale" content="ja_JP" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={pageDescription} />
    <meta name="twitter:image" content={new URL('/images/GHOSTS/logo.webp', Astro.site)} />
    <meta name="twitter:site" content="@M1RA_A_Project" />

    <!-- JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "CreativeWork",
      "name": pageTitle,
      "description": pageDescription,
      "url": new URL(Astro.url.pathname, Astro.site).toString(),
      "author": {
        "@type": "Person",
        "name": "大鹿ニク / M1RA"
      },
      "genre": "サイバーパンクアニメ",
      "inLanguage": "ja",
      "isPartOf": {
        "@type": "WebSite",
        "name": "GHOSTS",
        "url": new URL('/ghosts/', Astro.site).toString()
      }
    })} />
  </head>
  <body class="ghosts-body">
    <!-- Floating Logo -->
    <a href="/ghosts/" class="floating-logo">
      <img src="/images/GHOSTS/logo.webp" alt="GHOSTS" />
    </a>

    <main>
      <div class="novel-container">
    <!-- Header -->
    <section class="novel-header">
      <h1 class="page-title">GHOSTS - アニメ</h1>
      <p class="page-description">
        GHOSTSの世界が動き出す——ショートアニメと長編アニメで展開するプロジェクト
      </p>
    </section>

    <!-- Long Anime Section -->
    <section class="anime-category-section">
      <div class="category-header">
        <h2 class="category-title">長編アニメ</h2>
        <p class="category-description">本格的にGHOSTSの物語を描く、15～30分のエピソード形式作品</p>
      </div>

      <div class="episodes-section">
        {longAnimes.length > 0 ? (
          longAnimes.map((episode) => (
            <article class="episode-card">
              <div class="episode-header">
                {episode.isFree && <span class="episode-badge">無料公開</span>}
                <span class="episode-number">EP.{episode.number}</span>
              </div>
              <div class="episode-content">
                <h3 class="episode-title">{episode.title}</h3>
                <p class="episode-description">{episode.description}</p>
                <div class="episode-footer">
                  {episode.url ? (
                    <a href={episode.url} target="_blank" rel="noopener noreferrer" class="note-link">
                      <Icon icon="youtube-logo" size="1.2rem" />
                      <span>視聴する</span>
                      <span class="link-arrow">→</span>
                    </a>
                  ) : (
                    <div class="coming-soon-label">
                      <Icon icon="rocket-launch" size="1.2rem" />
                      <span>{episode.status}</span>
                    </div>
                  )}
                </div>
              </div>
            </article>
          ))
        ) : (
          <div class="coming-soon-message">
            <Icon icon="television" size="4rem" />
            <h3 class="coming-soon-subtitle">COMING SOON</h3>
            <p class="coming-soon-text">
              AI統治下の2163年、音楽が消された世界で立ち上がる4人の物語。<br>
              カタコンベの暗闇、ネオンに染まる都市、そして音楽の復活——<br>
              全てが動き出す長編アニメーションプロジェクトが始動予定です。
            </p>
          </div>
        )}
      </div>
    </section>

    <!-- Short Anime Section -->
    <section class="anime-category-section">
      <div class="category-header">
        <h2 class="category-title">ショートアニメ</h2>
        <p class="category-description">1～3分で楽しめる、GHOSTSの世界観を描いた短編アニメーション</p>
      </div>

      <div class="short-anime-grid">
        {shortAnimes.map((anime) => (
          <article class="short-anime-card">
            <div class="short-anime-header">
              <span class="short-anime-number">SHORT #{anime.number}</span>
              <h3 class="short-anime-title">{anime.title}</h3>
            </div>
            <div class="video-wrapper">
              <video
                controls
                preload="metadata"
                poster={`${anime.videoPath.replace('.mp4', '')}-poster.jpg`}
                class="short-anime-video"
              >
                <source src={anime.videoPath} type="video/mp4" />
                お使いのブラウザは動画の再生に対応していません。
              </video>
            </div>
          </article>
        ))}
      </div>
    </section>

    <!-- Back Link -->
    <div class="back-section">
      <a href="/ghosts/" class="back-link">← GHOSTSトップに戻る</a>
    </div>
  </div>
    </main>
  </body>
</html>

<style is:global>
  /* GHOSTS Body */
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  .ghosts-body {
    background: #0F0F1E;
    color: #E0E0E0;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    line-height: 1.6;
    overflow-x: hidden;
  }

  /* Floating Logo */
  .floating-logo {
    position: fixed;
    top: 2rem;
    left: 2rem;
    z-index: 100;
    transition: all 0.3s ease;
  }

  .floating-logo img {
    height: 60px;
    width: auto;
    filter: drop-shadow(0 0 10px rgba(255, 8, 68, 0.5));
    transition: all 0.3s ease;
  }

  .floating-logo:hover img {
    filter: drop-shadow(0 0 15px rgba(39, 225, 255, 0.8));
    transform: scale(1.05);
  }

  @media (max-width: 768px) {
    .floating-logo {
      top: 1rem;
      left: 1rem;
    }

    .floating-logo img {
      height: 44px;
    }
  }

  main {
    padding-top: 100px;
  }

  /* Scrollbar */
  ::-webkit-scrollbar {
    width: 10px;
  }

  ::-webkit-scrollbar-track {
    background: #0F0F1E;
  }

  ::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, #FF0844, #27E1FF);
    border-radius: 5px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(180deg, #27E1FF, #FF0844);
  }

  ::selection {
    background: #FF0844;
    color: white;
  }
</style>

<style>
  .novel-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem;
  }

  /* Header */
  .novel-header {
    text-align: center;
    margin-bottom: 4rem;
    padding: 2rem 0;
  }

  .page-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    color: #FF0844;
    margin-bottom: 1.5rem;
    letter-spacing: 0.2rem;
    text-transform: uppercase;
    text-shadow:
      0 0 20px rgba(255, 8, 68, 0.8),
      0 0 40px rgba(255, 8, 68, 0.5);
  }

  .page-description {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: #B0B0B0;
    line-height: 1.8;
  }

  /* Anime Category Section */
  .anime-category-section {
    margin-bottom: 5rem;
  }

  .category-header {
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid rgba(39, 225, 255, 0.2);
  }

  .category-title {
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 800;
    color: #27E1FF;
    margin-bottom: 0.75rem;
    letter-spacing: 0.1rem;
    text-shadow:
      0 0 15px rgba(39, 225, 255, 0.5),
      0 0 25px rgba(39, 225, 255, 0.3);
  }

  .category-description {
    font-size: clamp(0.9rem, 1.5vw, 1rem);
    color: #999;
    line-height: 1.6;
  }

  /* Episodes Section */
  .episodes-section {
    margin-bottom: 2rem;
  }

  /* Episode Card - コンパクト版 */
  .episode-card {
    background: rgba(15, 15, 30, 0.5);
    border-left: 3px solid rgba(255, 8, 68, 0.3);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
    position: relative;
  }

  .episode-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 3px;
    height: 100%;
    background: linear-gradient(180deg, transparent, #FF0844, transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .episode-card:hover::before {
    opacity: 1;
  }

  .episode-card:hover {
    background: rgba(15, 15, 30, 0.8);
    transform: translateX(5px);
  }

  .episode-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .episode-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: rgba(0, 255, 136, 0.2);
    border: 1px solid #00ff88;
    color: #00ff88;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    border-radius: 0.25rem;
  }

  .episode-number {
    font-size: 0.75rem;
    color: #666;
    font-weight: 600;
    letter-spacing: 0.1em;
  }

  .episode-content {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .episode-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: #FFFFFF;
    margin: 0;
    letter-spacing: 0.02em;
    transition: color 0.3s ease;
  }

  .episode-card:hover .episode-title {
    color: #27E1FF;
  }

  .episode-description {
    font-size: 0.9rem;
    color: #888;
    line-height: 1.6;
    margin: 0;
  }

  .episode-footer {
    display: flex;
    align-items: center;
    margin-top: 0.5rem;
  }

  .note-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #FF0844;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition: all 0.3s ease;
  }

  .note-link:hover {
    color: #27E1FF;
  }

  .link-arrow {
    transition: transform 0.3s ease;
  }

  .note-link:hover .link-arrow {
    transform: translateX(3px);
  }

  .coming-soon-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #FFB700;
    font-weight: 600;
    font-size: 0.8rem;
  }

  /* Coming Soon Message */
  .coming-soon-message {
    text-align: center;
    padding: 2.5rem 2rem;
    background: rgba(20, 20, 20, 0.4);
    border: 2px solid rgba(39, 225, 255, 0.2);
    border-radius: 0.5rem;
    margin-top: 1rem;
  }

  .coming-soon-message [astro-icon] {
    color: #27E1FF;
    opacity: 0.6;
    margin-bottom: 1rem;
  }

  .coming-soon-subtitle {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem);
    font-weight: 700;
    color: #FFB700;
    margin-bottom: 0.75rem;
    letter-spacing: 0.15rem;
    text-shadow:
      0 0 10px rgba(255, 183, 0, 0.4),
      0 0 20px rgba(255, 183, 0, 0.2);
  }

  .coming-soon-text {
    font-size: 0.9rem;
    color: #999;
    line-height: 1.8;
  }

  /* Back Section */
  .back-section {
    text-align: center;
    margin-top: 4rem;
  }

  .back-link {
    display: inline-block;
    padding: 1rem 2rem;
    background: rgba(39, 225, 255, 0.1);
    border: 2px solid rgba(39, 225, 255, 0.3);
    border-radius: 0.5rem;
    color: #27E1FF;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    letter-spacing: 0.1em;
    transition: all 0.3s ease;
  }

  .back-link:hover {
    background: rgba(39, 225, 255, 0.2);
    border-color: #27E1FF;
    box-shadow: 0 0 20px rgba(39, 225, 255, 0.3);
    transform: translateX(-5px);
  }

  /* Short Anime Section */
  .short-anime-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2.5rem;
    margin-top: 2rem;
  }

  .short-anime-card {
    background: rgba(15, 15, 30, 0.6);
    border: 2px solid rgba(255, 8, 68, 0.3);
    border-radius: 1rem;
    padding: 1.5rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .short-anime-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, transparent, #FF0844, #27E1FF, transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .short-anime-card:hover::before {
    opacity: 1;
  }

  .short-anime-card:hover {
    border-color: #FF0844;
    box-shadow: 0 0 30px rgba(255, 8, 68, 0.3);
    transform: translateY(-5px);
  }

  .short-anime-header {
    margin-bottom: 1rem;
  }

  .short-anime-number {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: rgba(255, 8, 68, 0.2);
    border: 1px solid rgba(255, 8, 68, 0.5);
    border-radius: 0.25rem;
    color: #FF0844;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
  }

  .short-anime-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: #FFFFFF;
    margin: 0.5rem 0 0 0;
    letter-spacing: 0.05em;
    transition: color 0.3s ease;
  }

  .short-anime-card:hover .short-anime-title {
    color: #27E1FF;
  }

  .video-wrapper {
    position: relative;
    width: 100%;
    border-radius: 0.5rem;
    overflow: hidden;
    background: #000;
    margin-bottom: 1rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
  }

  .short-anime-video {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 0.5rem;
  }

  .short-anime-video::-webkit-media-controls-panel {
    background: linear-gradient(180deg, transparent, rgba(15, 15, 30, 0.9));
  }

  /* Responsive */
  @media (max-width: 768px) {
    .novel-container {
      padding: 1rem;
    }

    .anime-category-section {
      margin-bottom: 4rem;
    }

    .category-header {
      margin-bottom: 1.5rem;
    }

    .episode-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .coming-soon-message {
      padding: 2rem 1.5rem;
    }

    .short-anime-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .short-anime-card {
      padding: 1.25rem;
    }
  }
</style>
